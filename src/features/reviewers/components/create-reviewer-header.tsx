const CreateReviewerHeader = () => {
    return (
        <div>
            <h1 className="text-2xl font-semibold">Create Reviewer</h1>
            <p className="text-sm text-muted-foreground mt-1">
                Add a new reviewer to the system, assign their expertise, and
                manage their role in the peer review process.
            </p>
        </div>
    );
};

export default CreateReviewerHeader;
