import Header from "@/components/header";

const SettingHeader = () => {
    return (
        <Header
            title="Settings"
            description="Manage system preferences, user roles, and configuration settings."
        />
    );
};

export default SettingHeader;
